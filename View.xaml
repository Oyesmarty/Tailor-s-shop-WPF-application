<UserControl x:Class="WpfApp1.View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#FFFFE3E3" >
        <DataGrid x:Name="Invoicetable" HorizontalAlignment="Center" Height="420" Margin="0,0,0,0" VerticalAlignment="Center" Width="749" SelectionChanged="DataGrid_SelectionChanged" Cursor="Hand"  CellStyle="{DynamicResource MaterialDesignDataGridCell}" GridLinesVisibility="All" RowBackground="#FFDCAEAE">
            <DataGrid.RowHeaderStyle>
                <Style/>
            </DataGrid.RowHeaderStyle>
            <DataGrid.Background>
                <SolidColorBrush Color="#FFFAFAFA" Opacity="0.5"/>
            </DataGrid.Background>
        </DataGrid>
        <TextBlock HorizontalAlignment="Left" Margin="230,10,0,0" Text=" ALL CUSTOMERS  Invoice RECORDS" TextWrapping="Wrap" VerticalAlignment="Top" Width="373" FontWeight="Bold" FontStyle="Normal" TextDecorations="Underline" FontSize="20"/>

    </Grid>
</UserControl>
